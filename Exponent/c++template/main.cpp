#include <iostream>
#include <bits/stdc++.h>

namespace solution_1 {

  void Solve() {

  }

}

int main() {
  // freopen("input.txt", "r", stdin);
  // freopen("output.txt", "w", stdout);
  std::ios::sync_with_stdio(false);
  // ios_base::sync_with_stdio(false);
  std::cin.tie(0);
  // std::cout.tie(0);

  long long tests_number = 1;

  // cin >> tests_number;
  // scanf("%lld", &tests_number);

  for (long long i = 0; i < tests_number; i++)
  {
    solution_1::Solve();
  }

  return 0;
}